from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Small resume project utilizing the hugginface library for nlp with focus on structure',
    author='Ansgar Kühn',
    license='MIT',
)
